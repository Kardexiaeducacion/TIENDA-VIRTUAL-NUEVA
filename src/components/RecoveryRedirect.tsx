"use client";
// This component is intentionally empty - the RecoveryRedirect hack is no longer needed.
// Password recovery now works correctly via the server-side /auth/callback route.
export default function RecoveryRedirect() {
  return null;
}
